!!! 5
%html{ lang: I18n.locale }
  %head
    %title= t '.title'
    = render partial: 'layouts/meta'
    = render partial: 'layouts/favicon'
    %style
      body { background: #000; }
      a { display: block; width: 50%; height: 50%; position: absolute; top: 0; bottom: 0; left: 0; right: 0; margin: auto; }
      svg { width: 100%; height: 100%; }
      svg path { fill: #fff; }
  %body
    %a{ :href => home_path }= dingbat_tag
